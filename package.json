{
  "name": "github-repo-guard",
  "version": "0.0.1",
  "description": "Webhook to enable branch protection on the default branch of a GitHub repository upon its creation",
  "type": "module",
  "scripts": {
    "lint": "npx eslint github-repo-guard --ext .js,.mjs && shellcheck util/*.sh",
    "test": "echo \"No tests yet...\""
  },
  "author": "",
  "dependencies": {
    "@octokit/core": "^3.5.1",
    "async-retry": "^1.3.3",
    "dotenv": "^14.1.0"
  },
  "devDependencies": {
    "eslint": "^8.7.0"
  }
}
